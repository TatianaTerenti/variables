Git instructions content:

Create a repository on GitHub
Copy url
cd folder-name (next), or cd .. (back) 
git init
git add .
git commit -m "Initial commit of homework"
git remote add origin [your-repository-url]
git branch -M main
git push -u origin main
*/














